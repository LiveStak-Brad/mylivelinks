# P0: Force legacy peer deps resolution for EAS cloud builds
# Required because @livekit/react-native and @livekit/react-native-webrtc
# have conflicting peer dependency requirements.
# DO NOT REMOVE - builds will fail on EAS without this.
legacy-peer-deps=true
