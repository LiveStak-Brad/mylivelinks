# BRANCH_POLICY

- Only `main` is allowed.
- No work on `crash-baseline-*` branches.
- All PRs/commits target `main`.
- If a branch is created temporarily, it must be merged back immediately and deleted.

