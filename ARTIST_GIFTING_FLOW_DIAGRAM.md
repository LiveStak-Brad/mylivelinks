# Artist Gifting - System Flow Diagram

## Complete User Journey

```
┌─────────────────────────────────────────────────────────────────────┐
│                         FAN USER JOURNEY                             │
└─────────────────────────────────────────────────────────────────────┘

1. DISCOVER
   ┌─────────────────┐
   │ Fan visits      │
   │ Artist Profile  │
   └────────┬────────┘
            │
            ▼
2. ENGAGE
   ┌─────────────────┐       ┌─────────────────┐
   │ Clicks "Music"  │  OR   │ Clicks "Videos" │
   │ section/tab     │       │ tab             │
   └────────┬────────┘       └────────┬────────┘
            │                         │
            ▼                         ▼
   ┌─────────────────┐       ┌─────────────────┐
   │  Music Player   │       │  Video Player   │
   │    Appears      │       │    Appears      │
   └────────┬────────┘       └────────┬────────┘
            │                         │
            ▼                         ▼
3. ENJOY
   ┌─────────────────┐       ┌─────────────────┐
   │ Plays music     │       │ Plays music     │
   │ track           │       │ video           │
   └────────┬────────┘       └────────┬────────┘
            │                         │
            └──────────┬──────────────┘
                       │
                       ▼
4. DISCOVER GIFT BUTTON
   ┌──────────────────────────────────┐
   │ Player Controls:                 │
   │ [⏮] [▶] [⏭] [🎁 GIFT]          │
   └────────┬─────────────────────────┘
            │
            ▼
5. DECIDE TO GIFT
   ┌─────────────────┐
   │ Fan clicks      │
   │ Gift Button     │
   └────────┬────────┘
            │
            ▼
6. GIFT MODAL OPENS
   ┌───────────────────────────────────┐
   │  Choose Your Gift                 │
   ├───────────────────────────────────┤
   │  Your Balance: 500 coins          │
   ├───────────────────────────────────┤
   │  ┌───┐ Rose       10 coins        │
   │  │🌹│ [Select]                    │
   │  └───┘                            │
   │  ┌───┐ Heart      20 coins        │
   │  │❤️│ [Select]                    │
   │  └───┘                            │
   │  ┌───┐ Diamond    50 coins        │
   │  │💎│ [Select]                    │
   │  └───┘                            │
   └───────────────────────────────────┘
            │
            ▼
7. SELECT GIFT
   ┌─────────────────┐
   │ Fan selects     │
   │ "Heart" gift    │
   └────────┬────────┘
            │
            ▼
8. CONFIRM
   ┌─────────────────┐
   │ Fan clicks      │
   │ "Send Gift"     │
   └────────┬────────┘
            │
            ▼
9. PROCESSING
   ┌──────────────────────────────────────┐
   │ API Call: /api/gifts/send            │
   │ {                                    │
   │   toUserId: "artist-id",             │
   │   coinsAmount: 20,                   │
   │   giftTypeId: 2                      │
   │ }                                    │
   └────────┬─────────────────────────────┘
            │
            ▼
10. TRANSACTION
    ┌──────────────────────────────────────┐
    │ Backend Processing:                  │
    │ 1. Validate sender ≠ recipient       │
    │ 2. Check coin balance                │
    │ 3. Deduct 20 coins from fan          │
    │ 4. Add 20 diamonds to artist         │
    │ 5. Create ledger entries             │
    │ 6. Update gifter level               │
    └────────┬─────────────────────────────┘
             │
             ▼
11. SUCCESS
    ┌──────────────────────────────────────┐
    │ ✓ Gift sent successfully!            │
    │ New balance: 480 coins               │
    └────────┬─────────────────────────────┘
             │
             ▼
12. MODAL CLOSES
    ┌──────────────────────────────────────┐
    │ Fan returns to music/video player    │
    │ Can continue enjoying content        │
    │ Can send more gifts if desired       │
    └──────────────────────────────────────┘
```

---

## Component Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         APP/PAGE LEVEL                               │
│  app/[username]/modern-page.tsx                                      │
│                                                                       │
│  - Fetches profile data                                              │
│  - Passes profile.id → artistProfileId                               │
│  - Passes profile.username → artistUsername                          │
│  - Determines if user is owner                                       │
└──────────────────────────────┬───────────────────────────────────────┘
                               │
               ┌───────────────┴───────────────┐
               │                               │
               ▼                               ▼
┌──────────────────────────┐   ┌──────────────────────────┐
│   MUSIC PLAYER LEVEL     │   │   VIDEO PLAYER LEVEL     │
│                          │   │                          │
│ MusicShowcase.tsx        │   │ MusicVideos.tsx          │
│ - Audio player UI        │   │ - Wrapper component      │
│ - Receives artist info   │   │ - Passes to player       │
│ - Shows gift button      │   │                          │
│ - Manages gift modal     │   │ VideoPlaylistPlayer.tsx  │
│                          │   │ - Video player UI        │
│                          │   │ - Shows gift button      │
│                          │   │ - Manages gift modal     │
└───────────┬──────────────┘   └───────────┬──────────────┘
            │                               │
            └───────────────┬───────────────┘
                            │
                            ▼
           ┌────────────────────────────────┐
           │      GIFT MODAL LEVEL          │
           │                                │
           │  GiftModal.tsx                 │
           │  - Gift selection UI           │
           │  - Balance display             │
           │  - Gift sending logic          │
           │  - API communication           │
           └────────────┬───────────────────┘
                        │
                        ▼
           ┌────────────────────────────────┐
           │        API LEVEL               │
           │                                │
           │  /api/gifts/send               │
           │  - Validates request           │
           │  - Calls RPC function          │
           │  - Returns result              │
           └────────────┬───────────────────┘
                        │
                        ▼
           ┌────────────────────────────────┐
           │      DATABASE LEVEL            │
           │                                │
           │  process_gift RPC              │
           │  - Updates profiles table      │
           │  - Creates ledger entries      │
           │  - Triggers gifter level calc  │
           └────────────────────────────────┘
```

---

## Data Flow

```
┌────────────────────────────────────────────────────────────────┐
│                        DATA FLOW                                │
└────────────────────────────────────────────────────────────────┘

PROFILE DATA (Down) ↓
────────────────────
Profile Page
  ├─ profile.id          → artistProfileId
  ├─ profile.username    → artistUsername
  └─ isOwnProfile        → isOwner

MusicShowcase / VideoPlayer
  ├─ artistProfileId    (stored)
  ├─ artistUsername     (stored)
  └─ isOwner            (controls button visibility)

GiftModal
  ├─ recipientId        (from artistProfileId)
  └─ recipientUsername  (from artistUsername)

API
  ├─ toUserId           (recipientId)
  └─ Validates & processes


GIFT ACTION (Up) ↑
─────────────────
User clicks Gift Button
  └─> Opens Modal

User selects gift
  └─> Highlights selection

User clicks Send
  └─> API Call

API processes
  └─> Database update

Success callback
  └─> Modal closes
      └─> User continues listening
```

---

## State Management

```
┌────────────────────────────────────────────────────────────────┐
│                      STATE DIAGRAM                              │
└────────────────────────────────────────────────────────────────┘

Player Component State
─────────────────────
┌──────────────────────┐
│ showGiftModal: false │  ← Initial State
└──────────┬───────────┘
           │
           │ (User clicks Gift Button)
           │
           ▼
┌──────────────────────┐
│ showGiftModal: true  │  ← Modal Open
└──────────┬───────────┘
           │
           │ (User sends gift OR closes modal)
           │
           ▼
┌──────────────────────┐
│ showGiftModal: false │  ← Back to Initial
└──────────────────────┘

GiftModal Internal State
────────────────────────
┌─────────────────────┐
│ selectedGift: null  │  ← Initial
└──────────┬──────────┘
           │ (User clicks gift)
           ▼
┌─────────────────────┐
│ selectedGift: {...} │  ← Gift Selected
└──────────┬──────────┘
           │ (User confirms)
           ▼
┌─────────────────────┐
│ loading: true       │  ← Sending...
└──────────┬──────────┘
           │ (API responds)
           ▼
┌─────────────────────┐
│ Success! Modal      │  ← Done
│ closes via callback │
└─────────────────────┘
```

---

## Conditional Logic

```
┌────────────────────────────────────────────────────────────────┐
│                    GIFT BUTTON VISIBILITY                       │
└────────────────────────────────────────────────────────────────┘

Should Gift Button Show?
───────────────────────

┌─────────────────┐
│ isOwner?        │
└────┬───────┬────┘
     │       │
    YES     NO
     │       │
     ▼       ▼
  [HIDE]  ┌─────────────────┐
          │ artistProfileId │
          │ exists?         │
          └────┬───────┬────┘
               │       │
              NO      YES
               │       │
               ▼       ▼
            [HIDE]  ┌─────────────────┐
                    │ artistUsername  │
                    │ exists?         │
                    └────┬───────┬────┘
                         │       │
                        NO      YES
                         │       │
                         ▼       ▼
                      [HIDE]  [SHOW]

Summary:
--------
SHOW if: !isOwner && artistProfileId && artistUsername
HIDE if: isOwner || !artistProfileId || !artistUsername
```

---

## Error Handling

```
┌────────────────────────────────────────────────────────────────┐
│                      ERROR SCENARIOS                            │
└────────────────────────────────────────────────────────────────┘

1. Insufficient Balance
   User tries to send gift → Check balance → Insufficient
   └─> Show error in modal: "Insufficient coin balance"
       └─> User can select cheaper gift or close modal

2. Network Error
   API call fails → Catch error
   └─> Show error in modal: "Failed to send gift. Please try again."
       └─> Transaction not recorded (safe failure)

3. Self-Gifting Attempt
   User tries to gift own profile → Backend validates
   └─> Return 400 error: "Cannot gift yourself"
       └─> (Should not happen due to UI hiding button)

4. Not Authenticated
   User not logged in → Gift modal requires auth
   └─> Redirect to login page (or show login prompt)

5. Server Error
   API returns 500 → Catch error
   └─> Show generic error: "Something went wrong. Please try again."
       └─> Log error for debugging

Error Recovery:
--------------
- Modal stays open on error
- User can retry or cancel
- No partial transactions
- Balance remains unchanged on failure
```

---

## Success Flow

```
┌────────────────────────────────────────────────────────────────┐
│                      HAPPY PATH                                 │
└────────────────────────────────────────────────────────────────┘

Fan → Music/Video → Gift Button → Modal → Select → Confirm → Success

Timeline:
─────────
T=0ms     : User clicks Gift Button
T=50ms    : Modal opens (smooth animation)
T=100ms   : Gift types loaded from DB
T=150ms   : User balance displayed
T=200ms   : User can interact with gifts

[User browses and thinks...]

T=5000ms  : User clicks on "Heart" gift
T=5050ms  : Gift highlights (visual feedback)
T=6000ms  : User clicks "Send Gift" button
T=6050ms  : Loading indicator shows
T=6100ms  : API call initiated

[Backend processing...]

T=6500ms  : Database updated
T=6550ms  : Response received
T=6600ms  : Success callback fires
T=6650ms  : Modal closes
T=6700ms  : User back at player

Total interaction time: ~6.7 seconds (including user think time)
Actual processing: ~600ms
```

---

**SYSTEM FLOW COMPLETE ✓**

This diagram covers the entire gifting flow from initial page load to successful gift transaction and return to content.

