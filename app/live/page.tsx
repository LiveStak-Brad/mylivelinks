'use client';

import LiveRoom from '@/components/LiveRoom';

export default function LivePage() {
  // LiveRoom component handles its own logic
  // goLive parameter can be checked via window.location.search if needed
  return <LiveRoom />;
}


